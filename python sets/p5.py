thisset = {"apple", "banana", "cherry"}
# To loop through the items in a set, you can use a for loop.

for x in thisset:
    print(x)
    
    
"""i = 0
while i < len(thisset):
    print(thisset[i])
    i+= 1"""
# Note: Sets are unordered, so you cannot be sure in which order the items will appear.
    
    